modules
paddles that can move vertically on key press
scoreboard
pong starts from center and that bounces in a random place after hitting paddles and walls and speed up pong when user scores




from turtle import Turtle, Screen

screen = Screen()
screen.setup(width=800, height=600)
screen.bgcolor("black")

paddle = Turtle()
paddle.shape("square")
paddle.penup()
paddle.goto(350, 0)
paddle.color("white")
paddle.turtlesize(5, 1, None)
screen.listen()
screen.tracer(0)



def go_up():
    new_y = paddle.ycor() + 20
    paddle.goto(paddle.xcor(), new_y)

def go_down():
    new_y = paddle.ycor() - 20
    paddle.goto(paddle.xcor(), new_y)


screen.onkeypress(go_up, "Up")
screen.onkeypress(go_down, "Down")

game_is_on = True
while game_is_on:
    screen.update()



screen.exitonclick()